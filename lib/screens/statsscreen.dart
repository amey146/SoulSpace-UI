import 'package:flutter/cupertino.dart';

class Statsscreen extends StatelessWidget {
  const Statsscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Stats to be displayed"));
  }
}
